<idea-plugin>
    <id>no.huzef.blackhole.sql-to-java</id>

    <name>SQL to Java</name>

    <vendor email="Huzefaadil23@gmail.com" url="https://github.com/DaH-uzi-efa/SQL_TO_JAVA_PLUGIN">
        Huzefa Adil
    </vendor>

    <description><![CDATA[
        <h2>SQL to Java DTO Generator</h2>
        <p>Automatically generates Java DTO classes from SQL CREATE TABLE statements.</p>

        <h3>Features</h3>
        <ul>
            <li>Paste SQL and instantly get Java classes with fields, constructors, getters, and toString()</li>
            <li>Detects shared columns across tables and offers to generate an abstract superclass with subclasses</li>
            <li>Generates Java records for simple tables (6 fields or fewer)</li>
            <li>Supports Unicode characters (Æ, Ø, Å, etc.) in column and table names</li>
            <li>Automatically adds required imports (e.g. java.time.LocalDate)</li>
            <li>Save generated classes directly to your project's source folder</li>
        </ul>

        <h3>Usage</h3>
        <ol>
            <li>Open the "SQL to Java" tool window from the sidebar</li>
            <li>Paste your CREATE TABLE statements</li>
            <li>Click "Convert to Java"</li>
            <li>Click "Save to Project" to create the dto package with all generated files</li>
        </ol>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <resource-bundle>messages.MyMessageBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow
                id="SQL to Java"
                factoryClass="no.huzef.blackhole.MyToolWindowFactory"
                icon="AllIcons.Toolwindows.ToolWindowPalette"/>
    </extensions>
</idea-plugin>